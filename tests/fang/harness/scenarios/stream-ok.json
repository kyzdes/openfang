{
  "name": "stream-ok",
  "doc": "Streaming path (F15, POST /message/stream). A scenario that is green on /message says NOTHING about /message/stream - this is the control for that branch.",
  "endpoints": [
    {
      "role": "primary",
      "port": 8972,
      "prefix": "/primary/v1",
      "open": true,
      "chunk_chars": 6,
      "chunk_delay_ms": 0,
      "steps": [
        { "type": "text", "content": "Streaming works fine, thanks for asking.", "usage": [8, 9] }
      ],
      "default_step": { "type": "text", "content": "still streaming.", "usage": [1, 1] }
    }
  ]
}
